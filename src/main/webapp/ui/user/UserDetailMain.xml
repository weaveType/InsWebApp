<?xml version="1.0" encoding="UTF-8"?>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:ev="http://www.w3.org/2001/xml-events" xmlns:w2="http://www.inswave.com/websquare"
	xmlns:xf="http://www.w3.org/2002/xforms">

	<head meta_screenName="개인정보 수정">
		<w2:type>COMPONENT</w2:type>
		<w2:buildDate />
		<w2:MSA />
		<xf:model>
			<w2:dataCollection baseNode="map">
				<w2:dataMap baseNode="map" id="dma_userInfoVo">
					<w2:keyInfo>
						<w2:key id="accountId" name="계정 ID" dataType="number"></w2:key>
						<w2:key id="name" name="사용자명" dataType="text"></w2:key>
						<w2:key id="career" name="경력수준" dataType="text"></w2:key>
						<w2:key id="preferredLocations" name="지역" dataType="text"></w2:key>
						<w2:key id="currentPosition" name="직무분야" dataType="text"></w2:key>
						<w2:key id="yearSalary" name="희망연봉" dataType="number"></w2:key>
						<w2:key id="email" name="이메일" dataType="text"></w2:key>
						<w2:key id="bio" name="자기소개" dataType="text"></w2:key>
						<w2:key id="testChecked" name="테스트완료여부" dataType="boolean"></w2:key>
						<w2:key id="isMbtiChecked" name="MBTI검사완료여부" dataType="number"></w2:key>
						<w2:key id="isCodeChecked" name="코드분석완료여부" dataType="number"></w2:key>
					</w2:keyInfo>
				</w2:dataMap>
			</w2:dataCollection>
		</xf:model>
		<w2:layoutInfo />
		<w2:publicInfo method="" />

		<style type="text/css"><![CDATA[
    
        /* 사용자 프로필 헤더 */
        .profile-header {
            background-color: white;
            border-radius: 12px;
            padding: min(30px, 4vw);
            margin-bottom: 24px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 20px;
        }

        .profile-info {
            display: flex;
            align-items: center;
            gap: 20px;
            flex: 1;
            min-width: 0;
        }

        .profile-avatar {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
            overflow: hidden; /* 이미지가 원형 영역을 벗어나지 않도록 */
        }

        .profile-career {
            margin: 4px 0 8px 0;
        }

        .profile-details h2 {
            font-size: clamp(20px, 3.5vw, 24px);
            font-weight: bold;
            color: #333;
            margin: 0 0 4px 0; /* 하단 마진 줄임 */
        }

        .status-badge {
            color: white;
            padding: 4px 8px;
            border-radius: 12px;
            font-size: clamp(11px, 1.5vw, 12px);
            font-weight: 500;
        }

        /* 정보 카드 그리드 */
        .info-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 20px;    
            margin-bottom: 32px;
        }


        .info-card {
            width: 100%;
            border: 1px solid #e0e0e0;
            border-radius: 12px;
            padding: 24px;

            /* 그림자·움직임 제거 */
            box-shadow: none;
            transition: none;
            cursor: default;
        }

        .info-card-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 16px;
        }

        .info-card-title {
            font-size: clamp(14px, 2vw, 16px);
            font-weight: 600;
            color: #666;
        }

        /* 카드 내부 정보 그리드 */
        .info-content-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 24px;
            margin-top: 16px;
        }

        .info-item {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .info-item-label {
            font-size: 14px;
            color: #666;
            font-weight: 500;
        }

        .info-item-value {
            font-size: 16px;
            color: #333;
            font-weight: 600;
        }

        /* 섹션 */
        .section {
            border-radius: 12px;
            padding: min(30px, 4vw);
            margin-bottom: 24px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
        }

        .section-title {
            font-size: clamp(18px, 3vw, 20px);
            font-weight: bold;
            color: #333;
            margin-bottom: 24px;
            text-align: center;
        }


        /* 반응형 디자인 */
        
        /* 태블릿 */
        @media (max-width: 768px) {
            .profile-header {
                flex-direction: column;
                text-align: center;
                gap: 15px;
            }

            .profile-info {
                flex-direction: column;
                text-align: center;
            }

            .info-content-grid {
                grid-template-columns: 1fr;
                gap: 15px;
            }
        }

        @media (max-width: 480px) {
            .profile-header, .section {
                padding: 20px;
            }

            .profile-avatar {
                width: 60px;
                height: 60px;
            }
        }
        ]]></style>
		<script lazy="false" type="text/javascript"><![CDATA[
scwin.onpageload = function () {
    // 부모 페이지의 dma_userInfoVo 데이터맵 참조
    var parentUserInfoVo = $p.parent().dma_userInfoVo;

    // 부모의 dma_userInfoVo 데이터를 자식 페이지의 dma_userInfoVo에 복사
    if (parentUserInfoVo) {
        dma_userInfoVo.setJSON(parentUserInfoVo.getJSON());
        // MBTI/코드분석 카드 상태 업데이트
        scwin.updateCardStatus(dma_userInfoVo);
    }
};

// PageFrame을 이용한 동적 콘텐츠 렌더링
scwin.updateCardStatus = function (userInfoData) {
    try {
        let isMbtiDone = userInfoData.get("isMbtiChecked") == 1;
        let isCodeDone = userInfoData.get("isCodeChecked") == 1;

        // 둘 다 완료되었을 경우 - 결과 페이지 로드
        if (isMbtiDone && isCodeDone) {
            section_title.setLabel("당신의 성향은...");
            pf_content.setSrc("ResultFrame.xml");
        } else {
            // 미완료시 액션 카드 페이지 로드
            section_title.setLabel("매칭에 필요한 정보를 입력해주세요.");
            pf_content.setSrc("ActionCardsFrame.xml");
        }
    } catch (err) {
        console.error("UserDetailMain.xml - updateCardStatus 오류:", err);
    }
};

scwin.getUserInfoByCookie = function () {
    var userInfoCookie = WebSquare.cookie.getCookie("userInfo");
    var userInfo = null;

    if (userInfoCookie) {
        try {
            userInfo = JSON.parse(userInfoCookie);
        } catch (e) {
            console.error("사용자 정보 파싱 오류:", e);
            userInfo = null;
        }
    }
    return userInfo;
}
// 사용자 정보 수정 페이지로 이동
scwin.btn_userUpdate_onclick = function () {
    try {
        $p.url("/InsWebApp/ui/user/updateUser.xml");
    } catch (e) {
        console.error("사용자 정보 수정 페이지 이동 오류:", e);
        alert("페이지 이동 중 오류가 발생했습니다.");
    }
}
// 페이지 새로고침을 위한 함수
scwin.refreshContent = function() {
    scwin.updateCardStatus(dma_userInfoVo);
};
]]></script>
	</head>

	<body ev:onpageload="scwin.onpageload">
		<xf:group class="main-wrap">
			<!-- 메인 컨테이너 -->
			<xf:group class="main-container">
				<!-- 사이드바 -->

				<!-- 메인 콘텐츠 -->
				<main class="main-content">
					<!-- 개인정보 카드 -->
					<section class="info-grid">
						<xf:group class="profile-info" style="">
							<xf:group class="profile-avatar">
								<xf:image src="/InsWebApp/images/ws5/default_user.png" style="width: 80px;height: 80px;" id=""></xf:image>
							</xf:group>
							<xf:group class="profile-details">
								<w2:textbox ref="data:dma_userInfoVo.name"
									style="font-size: 20px; font-weight: bold; color: #666; margin: 4px 0 8px 0;">
								</w2:textbox>
								<xf:group class="profile-career">
									<w2:textbox ref="data:dma_userInfoVo.career" style="font-size: 14px; color: #666; margin: 4px 0 8px 0;">
									</w2:textbox>
								</xf:group>
							</xf:group>
						</xf:group>
						<xf:group class="info-card" ev:onclick="scwin.editInfo_onclick('profile')">
							<xf:group class="info-card-header">
								<w2:textbox class="info-card-title" label="개인정보"></w2:textbox>
								<w2:textbox class="info-card-edit" label="✏️"
									style="color:#007bff;    font-size:clamp(18px,3vw,18px);    font-weight:700;    border:1px solid #ccc;    border-radius:12px;    aspect-ratio:1/1;    width: clamp(36px, 4vw, 56px);    display:flex;    align-items:center;    justify-content:center;    cursor:pointer;    text-decoration:none;"
									id="btn_userUpdate" ev:onclick="scwin.btn_userUpdate_onclick">
								</w2:textbox>
							</xf:group>

							<xf:group class="info-content-grid">
								<!-- 지역·직무 -->
								<xf:group class="info-item">
									<w2:textbox class="info-item-label" label="직무"></w2:textbox>
									<w2:textbox class="info-item-value" ref="data:dma_userInfoVo.currentPosition"></w2:textbox>
								</xf:group>

								<!-- 근무 지역 -->
								<xf:group class="info-item">
									<w2:textbox class="info-item-label" label="근무 지역"></w2:textbox>
									<w2:textbox class="info-item-value" ref="data:dma_userInfoVo.preferredLocations"></w2:textbox>
								</xf:group>

								<!-- 연봉 -->
								<xf:group class="info-item">
									<w2:textbox class="info-item-label" label="희망 연봉"></w2:textbox>
									<w2:textbox class="info-item-value" ref="data:dma_userInfoVo.yearSalary" displayFormat="#,###" id="yearSalary_textbox"></w2:textbox>
								</xf:group>

								<!-- 경력 수준 -->
								<xf:group class="info-item">
									<w2:textbox class="info-item-label" label="경력 수준"></w2:textbox>
									<w2:textbox class="info-item-value" ref="data:dma_userInfoVo.career"></w2:textbox>
								</xf:group>
							</xf:group>
						</xf:group>
					</section>

					<!-- 매칭에 필요한 정보 섹션 -->
					<section class="section">
						<w2:textbox id="section_title" class="section-title" label="매칭에 필요한 정보를 입력해주세요."></w2:textbox>

						<!-- PageFrame으로 동적 콘텐츠 렌더링 -->
						<w2:pageFrame id="pf_content" src="" style="width: 100%; min-height: 200px;"></w2:pageFrame>
					</section>
				</main>
			</xf:group>
		</xf:group>
	</body>
</html>